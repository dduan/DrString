name: Continuous Integration

on: [push]

jobs:
  macos:
    name: macOS
    runs-on: macos-latest
    strategy:
      matrix:
        action:
          - build
          - package-darwin
          - test
          - test-generated-artifacts
    steps:
    - uses: actions/checkout@v2
    - name: Xcode version
      run: sudo xcode-select -s /Applications/Xcode_13.2.1.app
    - name: Action
      run: make ${{ matrix.action }}

  ubuntu:
    name: Ubuntu
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - amd64
          #- arm64/v8
        swift:
          - 5.6
        ubuntu:
          - bionic
          - focal
        action:
          - build
          - package-linux
          - test
    steps:
    - uses: actions/checkout@v2
    - name: Action
      run: Scripts/ubuntu.sh ${{ matrix.action }} ${{ matrix.swift }} ${{ matrix.ubuntu }} ${{ matrix.arch }}
